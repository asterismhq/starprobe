services:
  research-api:
    build:
      target: development
    ports: !override
      - "${RESEARCH_API_HOST_IP:-0.0.0.0}:${RESEARCH_API_DEV_PORT:-8001}:8000"
    volumes:
      - ./src:/app/src
    environment:
      - CONTAINER_ENV=development
      - SEARXNG_SECRET=${SEARXNG_SECRET}
