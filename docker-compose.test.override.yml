version: "3.9"

services:
  research-api:
    build:
      target: development
    environment:
      - OLLAMA_HOST=http://ollama:11434/
      - CONTAINER_ENV=development
    depends_on:
      ollama:
        condition: service_healthy

  ollama:
    image: ghcr.io/famiclone/ol-ci-img:tinyllama-v1
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:11434/api/tags" ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  ollama-data:
    driver: local
    name: ollama-data
    external: false
